import { useState } from 'react';
import { NavLink } from 'react-router-dom';
import { useAuth } from '../hooks/useAuth';
import './Navbar.css';
const primary=[{to:'/',label:'Home'},{to:'/book',label:'Book'},{to:'/messages',label:'Messages'}];
const more=[{to:'/profile',label:'My profile'},{to:'/history',label:'History'},{to:'/products',label:'Products'},{to:'/promotions',label:'Promotions'},{to:'/requests',label:'Requests'},{to:'/emails',label:'Emails'},{to:'/settings',label:'Settings'}];
export function Navbar(){const{user,signOut}=useAuth();const[open,setOpen]=useState(false);const initial=(user?.email?.[0]??'C').toUpperCase();return <nav className="navbar"><div className="portal-nav-main"><NavLink className="navbar-brand" to="/"><img src="/logos/alessandroenterprises.png" alt="Alessandro Enterprises" className="brand-logo"/><span>Alessandro<br/><small>Customer portal</small></span></NavLink><div className="navbar-links">{primary.map(link=><NavLink key={link.to} to={link.to} end={link.to==='/'} className={({isActive})=>isActive?'navbar-link active':'navbar-link'}>{link.label}</NavLink>)}</div></div><div className="navbar-user"><button className="mini-profile" onClick={()=>setOpen(!open)} aria-expanded={open}><b>{initial}</b><span><strong>My account</strong><small>{user?.email}</small></span><em>⌄</em></button><button className="hamburger" aria-label="Open menu" onClick={()=>setOpen(!open)}>☰</button>{open&&<div className="portal-menu"><p>MENU</p>{more.map(link=><NavLink key={link.to} to={link.to} onClick={()=>setOpen(false)} className="portal-menu-link">{link.label}</NavLink>)}<a className="portal-menu-link" href="https://alessandros-enterprises.vercel.app" target="_blank" rel="noreferrer">Explore website ↗</a><a className="portal-menu-link" href="mailto:alessandrosenterprises@gmail.com">Email support</a><a className="portal-menu-link" href="https://wa.me/260768148043" target="_blank" rel="noreferrer">WhatsApp support</a><button className="portal-signout" onClick={()=>void signOut()}>Log out</button></div>}</div></nav>}
