import { useEffect, useState } from 'react';
import { supabase } from '../supabaseClient';
import './pages.css';
type Post={id:string;title:string;description:string;image_url:string|null;created_at:string};
export function Updates(){const[posts,setPosts]=useState<Post[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{async function load(){const{data}=await supabase.from('content_posts').select('*').eq('published',true).order('created_at',{ascending:false});setPosts((data??[])as Post[]);setLoading(false)}void load();const channel=supabase.channel('customer-content-posts').on('postgres_changes',{event:'*',schema:'public',table:'content_posts'},()=>void load()).subscribe();return()=>{void supabase.removeChannel(channel)}},[]);return <div className="page"><p className="eyebrow">FROM ALESSANDRO ENTERPRISES</p><h1>Latest updates</h1><p className="page-subtitle">News, announcements, and featured posts from our team.</p>{loading?<div className="page-loading">Loading updates...</div>:<div className="post-grid">{posts.map(post=><article className="post-card" key={post.id}>{post.image_url&&<img src={post.image_url} alt=""/>}<div><h2>{post.title}</h2><p>{post.description}</p><small>{new Date(post.created_at).toLocaleDateString()}</small></div></article>)}{posts.length===0&&<p className="page-empty">No posts have been published yet.</p>}</div>}</div>}
