import { useEffect, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { getActiveServices } from '../services/services';
import type { Service } from '../types';
import { formatZMW } from '../utils/currency';
import './pages.css';
export function Products(){const[services,setServices]=useState<Service[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const navigate=useNavigate();useEffect(()=>{getActiveServices().then(({data,error})=>{setServices(data??[]);setError(error?.message??null);setLoading(false)})},[]);return <main className="page"><p className="eyebrow">DISCOVER</p><h1>Products &amp; Services</h1><p className="page-subtitle">Explore all our business units and solutions designed to make life and work easier.</p>{loading&&<div className="page-loading">Loading services...</div>}{error&&<div className="page-error">{error}</div>}<div className="service-catalogue">{services.map(service=><article className="card" key={service.id}><h3>{service.name} <small>›</small></h3><p>{service.description||'Professional service from Alessandro Enterprises.'}</p>{Number(service.price)>0&&<p className="card-meta">{formatZMW(service.price)}</p>}<div className="service-card-actions"><button className="btn" onClick={()=>navigate(`/products/${service.id}`)}>View service</button><button className="btn btn-secondary" onClick={()=>navigate('/book',{state:{serviceId:service.id}})}>Book</button></div></article>)}</div>{!loading&&!services.length&&<p className="page-empty">No services available right now.</p>}</main>}
